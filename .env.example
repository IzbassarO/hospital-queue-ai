# Copy to .env and adjust. Used by docker-compose, backend and ml pipelines.
POSTGRES_USER=hqai
POSTGRES_PASSWORD=change-me
POSTGRES_DB=hqai
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# DuckDB resources for the ingest pipeline
DUCKDB_MEMORY_LIMIT=4GB
