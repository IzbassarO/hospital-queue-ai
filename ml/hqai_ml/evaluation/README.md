# hqai_ml.evaluation

Implemented. `split.py` temporal split and early-stopping holdout (never random); `metrics.py` regression, classification, calibration and segment metrics; `backtest.py` rolling-origin runner and forecast error tables; `feature_notes.py` leakage review per feature; `report.py` renders reports/02_models.md from the metrics of the current registry versions.
